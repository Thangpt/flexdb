CREATE OR REPLACE FORCE VIEW V_RM_GETREQUEST4RECONCIDE AS
SELECT AUTOID, TRNREF,
    TO_CHAR(TXDATE,'DD/MM/RRRR') TXDATE,
    BANKCODE, ACCNUM, ACCNAME,
    AMOUNT, ERRORDESC,
    CASE WHEN STATUS = 'C' THEN '0' ELSE '1' END STATUS
from TCDTRECEIVEREQUEST
WHERE STATUS IN ('C','E','H');

